<p id="notice"><%= notice %></p>

<h1>News Items</h1>

<table>
  <thead>
    <tr>
      <th>News site</th>
      <th>Title</th>
      <th>Description</th>
      <th>Link</th>
      <th>Published at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @news_items.each do |news_item| %>
      <tr>
        <td><%= news_item.news_site_id %></td>
        <td><%= news_item.title %></td>
        <td><%= news_item.description %></td>
        <td><%= news_item.link %></td>
        <td><%= news_item.published_at %></td>
        <td><%= link_to 'Show', news_item %></td>
        <td><%= link_to 'Edit', edit_news_item_path(news_item) %></td>
        <td><%= link_to 'Destroy', news_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New News Item', new_news_item_path %>

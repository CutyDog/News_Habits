<%= form_for(news_site) do |f| %>
  <% if news_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_site.errors.count, "error") %> prohibited this news_site from being saved:</h2>

      <ul>
      <% news_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :rss_url %>
    <%= f.text_area :rss_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
